<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Booking a car</title>
    <link rel="stylesheet" href="/css/main.css" />
</head>
<body>     
    <div class="container mx-auto h-screen flex justify-center items-center">
        <div class="w-1/3">
            <h1 class="my-6 text-2xl">Book</h1>
            <form action="/book/<%= car._id %>" method="POST" class="flex flex-col gap-2 mb-6">
                <input type="date" name="date" min="<%= new Date().toISOString().split('T')[0] %>" class="input input-bordered w-full" required/>                
                <button class="btn btn-active btn-neutral">Book the Car</button>       
            </form>            
        </div>            
    </div>
</body>
</html>